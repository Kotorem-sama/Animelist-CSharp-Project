<Page x:Class="Animelist_CSharp_Project.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Animelist_CSharp_Project"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegisterPage">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries >
                <ResourceDictionary Source="XamlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <!--The catgirl silhouette gets added first, because otherwise it would be layered above everything-->
        <Image Source="./Images/CatGirlSilhouette2.png" Style="{DynamicResource Silhouettes}"/>
        
        <!--Back button-->
        <Button Click="GoBackButton_Click" Style="{DynamicResource BackButtonStyle}">
            <Image Style="{DynamicResource BackButtonImageStyle}"/>
            <Button.Background>
                <SolidColorBrush Opacity="0" />
            </Button.Background>
        </Button>

        <!--Register title text-->
        <Label Content="Register" FontSize="40" Margin="0,-240,0,0" Style="{DynamicResource LabelStyle}"/>

        <!--Error messages-->
        <Label Visibility="Hidden" Name="AlreadyInUse" Content="This Username or Email is already in use." Foreground="#FFCB5B" Style="{DynamicResource LabelStyle}"/>
        <Label Visibility="Hidden" Name="PasswordsNoMatch" Content="These Passwords don't match." Foreground="#FF0008" Style="{DynamicResource LabelStyle}"/>
        <Label Visibility="Hidden" Name="EmptyError" Content="Please fill in every input box." Foreground="#FF0008" Style="{DynamicResource LabelStyle}"/>
        <Label Visibility="Hidden" Name="Successfull" Content="Your account has been successfully created." Foreground="#28FF07" Style="{DynamicResource LabelStyle}"/>

        <!--Input boxes-->
        <TextBox Name="EmailTextBox" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,-105,0,0" Text="Email" Style="{DynamicResource TextBoxes}" />
        <TextBox Name="UsernameTextBox" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,-29,0,0" Text="Username" Style="{DynamicResource TextBoxes}" />
        <PasswordBox Name="PasswordTextBox" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,45,0,0" Password="Password" Style="{DynamicResource TextBoxes}" />
        <TextBox Name="PasswordTextBoxShow" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,45,0,0" Text="Password" Visibility="Hidden" Style="{DynamicResource TextBoxes}" />
        <PasswordBox Name="ConfirmPasswordTextBox" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,121,0,0" Password="Password" Style="{DynamicResource TextBoxes}" />
        <TextBox Name="ConfirmPasswordTextBoxShow" LostKeyboardFocus="LostKeyFocus" GotKeyboardFocus="GotKeyFocus" Margin="0,121,0,0" Text="Password" Visibility="Hidden" Style="{DynamicResource TextBoxes}" />

        <!--Defines the show password button for the password box-->
        <Button Name="ShowPasswordButton" Click="ShowPasswordButtonEvent" Margin="200,45,0,0"  Style="{DynamicResource ShowPasswordButtonStyle}">
            <Image Name="ShowPasswordImage" Style="{DynamicResource ShowPasswordImageStyle}"/>
            <Button.Background>
                <SolidColorBrush Opacity="0"/>
            </Button.Background>
        </Button>

        <!--Defines the show password button for the confirm password box-->
        <Button Name="ShowConfirmPasswordButton" Click="ShowConfirmPasswordButtonEvent" Margin="200,120,0,0" Style="{DynamicResource ShowPasswordButtonStyle}">
            <Image Name="ShowConfirmPasswordImage" Style="{DynamicResource ShowPasswordImageStyle}" />
            <Button.Background>
                <SolidColorBrush Opacity="0"/>
            </Button.Background>
        </Button>
        
        <!--Register button-->
        <Button Name="RegisterButton" Click="RegisterButton_Click" Content="Register" Margin="0,220,0,0" Style="{DynamicResource Buttons}" />
    </Grid>
</Page>
